# Security and Privacy

Do not open public issues containing credentials, private host details, raw
honeypot records, or unredacted indicators. The public project intentionally
uses aliases and summarized event data.

The repository must never contain `.env` files, API keys, private addresses,
SSH material, service credentials, raw logs, or deployment inventory.
